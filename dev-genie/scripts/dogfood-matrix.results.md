# Dogfood matrix results

Generated by `node dev-genie/scripts/dogfood-matrix.mjs` on 2026-05-08.

## Matrix

| fixture                   | repo shape                                                                  | result | notes                                                                          |
| ------------------------- | --------------------------------------------------------------------------- | ------ | ------------------------------------------------------------------------------ |
| greenfield                | empty repo, no manifests                                                    | PASS   | no eslint / tsconfig / agent configs detected                                  |
| claude-locked             | `package.json` + `eslint.config.mjs` + `tsconfig.json` + locking `CLAUDE.md`| PASS   | lock on `eslint.config.mjs` correctly captured by `findLockForPath`            |
| vercel-style-guide        | `eslint.config.mjs` extending `@vercel/style-guide`                         | PASS   | flat config detected; lint script detected                                     |
| claude-locked-and-vercel  | both above, with fenced `locked: [eslint.config.*, tsconfig.json]` block    | PASS   | both glob locks match the actual files                                         |
| husky-managed             | `.husky/pre-commit` only (no native git hook)                               | PASS   | `hooks.husky=true`, `hooks.nativePreCommit=false`                              |

## What is verified

- Detection report shape matches `existing-config-detection` SKILL contract on each fixture.
- Agent-config lock parser captures both phrase-based ("Do not modify `eslint.config.mjs`.") and fenced `locked:` YAML blocks.
- Glob expansion in `findLockForPath` correctly matches `eslint.config.*` to `eslint.config.mjs`.
- Hook framework probe distinguishes `husky` vs `nativePreCommit`.

## What is not verified by the matrix script

- End-to-end `applyFindings` against fixtures (would require a baseline + comparator round-trip per arch). This is exercised by the existing `compare-config.js` smoke-test against the dev-genie repo itself. The full apply path including lock-resolution prompts is interactive and best validated against a real repo.
- `eslint --print-config` fallback (requires `eslint` to be installed in the fixture). Documented as a soft dependency in the SKILL contract.

## Idempotent re-run

The matrix script is idempotent: each fixture is created fresh per run in a temp directory, so repeated invocations always produce the same results. `.dev-genie/init.last-run.json` round-tripping is exercised by `dev-genie/lib/plan-store.test.mjs`.

## Blockers / follow-ups

- None. All 5 fixtures passing as of this run.
