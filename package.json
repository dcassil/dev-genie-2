{
  "name": "dev-genie-workspace",
  "private": true,
  "packageManager": "pnpm@10.33.1",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "release:plugin": "node scripts/bundle-plugin.mjs",
    "test:release": "node --test scripts/bundle-plugin.test.mjs"
  }
}
